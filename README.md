# WSL2 Debian Setup

Run inside Debian WSL after install.

```bash
./run_all.sh
source ~/.bashrc
tmux            # then prefix + I for plugins
nvim            # plugins auto-install on first launch
```
